<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/smsFilterLayout"
    android:layout_width="@dimen/smsFilterItemWidth"
    android:layout_height="35dp"
    android:background="@color/darkWhite"
    android:layout_marginTop="10dp"
    android:paddingStart="5dp"
    android:layout_marginStart="5dp"
    android:layout_marginEnd="5dp"
    android:orientation="horizontal"


    >

        <TextView
            android:id="@+id/smsFilter"
            android:layout_width="wrap_content"
            android:layout_height="25dp"
            android:textColor="@color/black"
            android:text="Filter Criteria"
            android:layout_centerVertical="true"
            android:textSize="17sp"


            />

        <ImageView
            android:id="@+id/smsFilterDeleteButton"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:src="@android:drawable/ic_delete"
            android:contentDescription="Delete Filter"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:clickable="true"
            android:focusable="true"

            />

</RelativeLayout>