<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/navigationDrawer"
    android:layout_width="300dp"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="@android:color/white"
    android:orientation="vertical"
    >

    <!-- Add your navigation menu items here -->
    <!-- Example: TextView, ListView, etc. -->
    <!-- For example, you can use a ListView to display a list of items -->
    <ListView
        android:id="@+id/navigationListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="?attr/textColorPrimary"
        android:dividerHeight="1dp"
        android:padding="8dp"
        android:entries="@array/navigation_items" />
</LinearLayout>